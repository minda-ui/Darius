# Change-log index — Workshop of Furniture Making KB

**Every session's `change-log-*.md` file, newest first.** Split out of `Outputs/kb-registers.md` at
**v28** (2026-09-21) because that file was re-emitted whole every session and 35,701 bytes of it was
append-only history. **Period-split in turn on 2026-09-23** (owner's instruction, *"split by period"*),
once the index itself reached 46,462 bytes: the same problem one file along, and the same fix.

**This file holds 2026-09-23 onwards.** Earlier sessions are frozen and never re-emitted:

| Period file | Holds |
|---|---|
| `Outputs/change-log-index-2026-09-14-to-09-21.md` | Sessions 1 to 19, newest first |

**This is the file `CLAUDE.md` §0 sends you to**, second in the session-start reading order: read the
newest row, then the open `Processed items` rows of `kb-registers.md`. **When this file passes roughly
25 KB, freeze it as the next period.**

**Nothing here was rewritten** at either split. Rows moved verbatim, in the same order, by script.

---

## Change-log entries

Newest first.

| Date | Entry | File |
|---|---|---|
| 2026-09-23 | Session 20 - **T016's root cause was wrong for eight days, and the answer was on the shop's own hard drive.** **(1) The correction.** Since 2026-09-15 this KB has said *"Tool for this working not found"* means the optimiser found several matching tools and could not choose. **TPA's `CnCadOpti_eng.pdf` documents error -27, *"tool match not possible - No tool can be used"*** - raised when **nothing** matches; §1.5 checks for *"at least one tool"* and §1.6 aggregates across candidates deliberately. **The ambiguity failure mode does not exist.** The 15 September observations were real; the mechanism joining them was invented and never checked against a document, because the document was believed not to exist. **The five criteria** now recorded as a checklist: diameter, tool type *(only if working sets type > 0)*, face, **useful length >= programmed depth**, axis limits - fail one for every tool and you get -27, and the message does not say which. **(2) How it was found - checklist step A17.** `C:\Albatros\help\en-GB\` holds **29 files**: `Workings_eng.pdf` (337 pp), the complete `TpaCAD_eng.pdf` (7.7 MB), `DxfCAD_eng.pdf`, `DxfToTpa_eng.pdf` - **all three siblings the 2026-09-15 article named, plus the manual its own first page pointed at. T015 closes.** But the file that broke T016 open was `CnCadOpti_eng.pdf`, 19 pages, **which nobody knew existed**. *We went looking for the manual we had been told about; the answer was in the folder next to it.* Both byte-verified in and extracted locally. **(3) `03-BOTTOMB.TCN`, face 1 working 11** - `TD3 TP0`, `Z-13.0` in 19 mm, so blind and programmed by diameter. **Ø3 is only `passante` on this head** (owner, from the archive), so criterion 2 fails for every tool. **A tooling/design mismatch, not a software fault** - SmartCABINET exports a hole the machine cannot drill blind. On 21 September `03-BOTTOM.TCN` had solved **clean**, and I came close to writing *"T016 does not reproduce"* - the v28 tense lesson, two days old, earning itself. **(4) The head, and it changed twice in two days.** Bushes **6-10 confirmed as five blind Ø5** - September's claim checked against the archive for the first time, and **vindicated**; so only its mechanism was invented. Why Ø5 failed is now **unexplained**, useful-length the leading hypothesis, **recorded as a hypothesis**. Ø10 and Ø12 **drills** fitted 22 September - not cutters, so the interpolated-hole test **stays blocked**, correcting a suggestion I had made an hour earlier. I had also written *"nothing suggests the outfit has changed since September"*; something did, and I had not asked. **(5) A latent fault with no error attached** - until 23 September **bush 3 held a blind bit while the archive said `passante`**. Every program resolving through-Ø3 there drilled with the wrong geometry and **nothing would have reported it**. Owner corrected the bit. **Kept separate from the -27**, which it did not cause - merging them would give this KB a *third* wrong cause for one message. **A tool change is finished when the archive says what is in the spindle, not when the drill is in it.** **(6) What the manuals settled**: `CABINEO` is **native** (*"3 holes with a 15 mm diameter executed up to a 11 mm depth"*, and the head has no Ø15, so routing stays the only route); `CIRCULAR INTERNAL WINDOW` appears **nowhere in 337 pages** so it is a custom working - but standard `CIRCLE` shows the mechanism (`[R] Radius` + separate `[DN] Compensation`), so **look at the working's own fields before cutting**; **there is no `THREE HOLES HINGE` table** and this KB should stop expecting one. Still open: whether `Z` is to the drill's shoulder or its tip. **(7) Two harmful instructions caught.** Error **-25** means the old fix's *"leave `Tool type` alone"* produces a **new** error, so it looks like it failed when it was never tried; and the procedure is **silent on manual p.63** - through-drills in the indicated spindles, black ring right-hand, red ring left-hand. **My own checklist stop rule 3 also had to be narrowed**, flagged to the owner rather than passed over. **(8) Both wrong articles kept**, corrected in place and visibly, bodies untouched below the banners. **(9) Two §3 candidates offered, not added**: *"confirmed on two X" is one observation and one assumption sharing a sentence*; and *a limitation is a property of the call you made* - **second instance in five days, first outside my own tooling**. **(10) Rule D adopted as Rule D, not Rule C.** The `Raw/` note said *"fold into your charter §0"*, which would have collided with the Rule C adopted here at v29; its own Hub row `AWT-0065` said *"under its OWN heading - do NOT relabel or overwrite"*, and **Rule A makes the row the canonical brief**, so the row governed and the owner confirmed directly. **First time §6a-i caught a real error rather than confirming a correct note.** **(11) The charter split - `CLAUDE.md` -> v31, four files** (`AWT-0082`, Alex, closed Done). **Measured before planning**: §7 was 34,789 B (40%) and §3 23,651 B (27%) of the 86,856 B monolith - 67% between them and **neither is a rule**, so the proposed *core/rules/history* cut would have left ~58 KB in "core" and saved little. Adopted the principle instead - **split by update cadence**: `CLAUDE.md` 19,468 (core), `CLAUDE-Rules.md` 11,118 (§0b + §6), `CLAUDE-Lessons.md` 24,221 (§3), `CLAUDE-Workshop.md` 35,378 (§7) = 90,185 B, **+3,329 for four headers and file maps, paid once**; a rule change now re-emits 10.5 KB instead of 86.9. **Section numbers kept** so every citation still resolves. Done by script with a reconstruction `assert` and **per-section sha256** - *a byte count cannot see a section that moved*. All four verified on Drive by download-decode-diff, `README.md` re-issued with them, the monolith archived intact. **The first publish came back one byte short and the spare byte was in the LOCAL copy** - a size check alone would have read it as a transfer loss. **Not solved, and said so on the row**: `CLAUDE-Workshop.md` is still 35 KB because §7 summarises live data held elsewhere; shrinking it is an owner decision needing its own measurement. **(12) The registers period-split, and a governance question answered.** §8e proposed it; the owner said do it (*"split by period"*). `kb-registers.md` **73,220 -> 15,236** and this index **46,462 -> 7,337**, settled rows moved verbatim into two frozen `-<period>.md` files (61,233 and 39,832). **Upkeep on these two drops from ~120 KB a session to ~23 KB**, with a written freeze rule at ~25 KB. **The boundary is *settled*, not old**: Processed items by **status**, the other tables by **date**, and the script asserts the two coincide. **119 rows in, 119 out, multiset identical**; 20 index rows checked row-for-row — *a byte count cannot catch a row that moved between files.* **Sweep first** (the v28 rule): §0 and `README.md` needed no change; two `CLAUDE-Lessons.md` mentions and one Decisions article left alone as dated records. `CLAUDE.md` is **v31 amended, not v32**, and says why. **And the owner ruled that the Machinery Register is an asset register, not a financial document** — the estate-law note's never-git rule does not reach it, so it stays in the mirror (`CLAUDE-Rules.md` §6b) | `Outputs/change-log-2026-09-23-t016-root-cause-corrected.md` |
